# Assets

This directory is reserved for OPL Cloud product maps, architecture diagrams,
and public screenshots.

The initial documentation uses Mermaid diagrams directly in Markdown so GitHub
can render them without generated image files.

